services:
  - type: web
    name: legal-memory-api
    runtime: python
    pythonVersion: "3.11"
    region: oregon
    plan: free
    buildCommand: pip install -r requirements-api.txt
    startCommand: python -m uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPERMEMORY_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
    healthCheckPath: /health
